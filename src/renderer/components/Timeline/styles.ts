import styled from '@emotion/styled'

export const Container = styled.div`
  display: flex;
`

export const Track = styled.div`
  position: relative;
  flex-grow: 1;
`

export const HourTrack = styled.ol`
  margin: 0;
  padding: 0;
  list-style: none;
`

export const EventTrack = styled.ol`
  margin: 0;
  padding: 0;
  list-style: none;
`

export const Sidebar = styled.div`
  width: 4rem;
`
